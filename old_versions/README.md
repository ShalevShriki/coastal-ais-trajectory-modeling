# Archived (not part of the submission layout)

Moved here during cleanup so the repo root only reflects the **final coastal report** pipeline (`exp_coastal`).

| Folder | Contents |
|--------|----------|
| `reports/` | Draft PDFs, LyX, older TeX, guidelines, feedback notes |
| `docs_notes/` | Research MD drafts, REPORT*.md, CLAUDE.md, loss notes |
| `scripts/` | `exp_final`, `exp_clean`, `exp_context`, `experiment1`, `ar_experiments`, old sbatch / audits |
| `models/` | Unused trainers (e.g. linear regression) |
| `logs/` | Slurm `LOG/` from development runs |
| `data_results/` | Pre-coastal result trees (v1/v2/`exp_final`/…) |
| `data_models/` | Matching old checkpoints |
| `data_processed/` | Intermediate `combined` / `combined_filtered` (pre-smart) |
| `misc/` | One-off helpers |

Final PDF source of truth remains at repo root: `report/AIS_report.pdf`.
